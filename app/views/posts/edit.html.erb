<%= form_for @post do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name, :size => 140 %> 
    <%= f.label :comment %>
    <%= f.text_field :comment, :size => 140 %>

    <%= f.submit "編集する" %>
    <%= link_to "投稿一覧に戻る", posts_path %>
  <% end %>